# 다중 알고리즘 WADI 보안 실험 가이드

## 🎯 **실험 개요**

WADI (Water Distribution) 데이터셋을 사용하여 4가지 주요 암호화 알고리즘의 성능을 비교 분석하는 종합 실험입니다.

### 📊 **실험 대상 알고리즘**

| 알고리즘 | 타입 | 키 크기 | 특징 |
|----------|------|---------|------|
| **HMAC-SHA256** | 메시지 인증 | 328비트 | 빠른 속도, 낮은 CPU 사용량 |
| **RSA-2048** | 공개키 서명 | 2048비트 | 높은 보안성, 느린 속도 |
| **AES-256-GCM** | 인증 암호화 | 256비트 | 기밀성 + 무결성 동시 제공 |
| **ECDSA-P256** | 타원곡선 서명 | 256비트 | RSA 대비 빠른 공개키 암호 |

## 🚀 **실험 실행 방법**

### **1단계: 개별 알고리즘 테스트**

각 알고리즘을 개별적으로 테스트할 수 있습니다:

```bash
# HMAC 실험
python complete_16_conditions_experiment.py

# RSA 실험  
python rsa_experiment.py

# AES-GCM 실험
python aes_gcm_experiment.py

# ECDSA 실험
python ecdsa_experiment.py
```

### **2단계: 통합 비교 실험**

모든 알고리즘을 한 번에 비교 실험:

```bash
python multi_algorithm_experiment.py
```

## 📈 **실험 조건**

### **테스트 매트릭스**

| 센서 개수 | 주파수 (Hz) | 요청 수 | 예상 소요 시간 |
|-----------|-------------|---------|----------------|
| 1개 | 1, 10 | 100개 | 1~10초 |
| 10개 | 1, 10 | 200~1,000개 | 2~10초 |
| 50개 | 10 | 2,000개 | 4초 |

**총 5개 조건 × 4개 알고리즘 = 20개 테스트**

### **측정 지표**

1. **인증 성능**
   - 인증 데이터 생성 시간 (ms)
   - 네트워크 RTT (ms)
   - 총 처리 시간 (ms)

2. **시스템 성능**
   - 성공률 (%)
   - 검증률 (%)
   - RPS (Requests Per Second)

3. **보안성**
   - 암호화 강도
   - 키 관리 복잡성
   - 공개키/대칭키 특성

## 🔧 **기술적 세부사항**

### **HMAC-SHA256**
```python
# 메시지 형식
message = f"{sensor_id}|{timestamp}|{value:.6f}"

# HMAC 생성
hmac_value = hmac.new(key, message.encode(), hashlib.sha256).hexdigest()
```

### **RSA-2048**  
```python
# RSA-PSS 서명 생성
signature = private_key.sign(
    message_bytes,
    padding.PSS(mgf=padding.MGF1(hashes.SHA256()), 
               salt_length=padding.PSS.MAX_LENGTH),
    hashes.SHA256()
)
```

### **AES-256-GCM**
```python
# AES-GCM 암호화 (인증 태그 포함)
ciphertext = aes_gcm.encrypt(nonce, message_bytes, aad)
```

### **ECDSA-P256**
```python
# ECDSA 서명 생성
signature = private_key.sign(message_bytes, ec.ECDSA(hashes.SHA256()))
```

## 📂 **결과 파일 구조**

```
../results/
├── multi_algorithm_comparison/
│   ├── detailed_results_YYYYMMDD_HHMMSS.csv
│   └── algorithm_summary_YYYYMMDD_HHMMSS.csv
├── hmac_experiment/
│   └── final_16_conditions_YYYYMMDD_HHMMSS.csv
├── rsa_experiment/
│   └── rsa_results_final_YYYYMMDD_HHMMSS.csv
├── aes_gcm_experiment/
│   └── aes_gcm_results_final_YYYYMMDD_HHMMSS.csv
└── ecdsa_experiment/
    └── ecdsa_results_final_YYYYMMDD_HHMMSS.csv
```

## 🎯 **예상 결과**

### **성능 예측**

| 알고리즘 | 인증 시간 | 검증률 | 적합한 환경 |
|----------|-----------|--------|-------------|
| HMAC | 0.02-0.05ms | 100% | IoT, 실시간 시스템 |
| ECDSA | 0.5-2ms | 99%+ | 모바일, 경량 PKI |
| AES-GCM | 0.1-0.3ms | 99%+ | 기밀성 중요 환경 |
| RSA | 5-20ms | 99%+ | 레거시, 고보안 환경 |

### **권장사항**

- **실시간 IoT**: HMAC-SHA256 (최고 속도)
- **모바일/임베디드**: ECDSA-P256 (효율적 공개키)
- **데이터 암호화**: AES-256-GCM (기밀성 + 인증)
- **높은 호환성**: RSA-2048 (표준 준수)

## 🔍 **서버 요구사항**

실험을 위해서는 다음 엔드포인트를 지원하는 서버가 필요합니다:

- `POST /api/v1/verify/hmac` - HMAC 검증
- `POST /api/v1/verify/rsa` - RSA 서명 검증  
- `POST /api/v1/verify/aes_gcm` - AES-GCM 복호화
- `POST /api/v1/verify/ecdsa` - ECDSA 서명 검증

## ⏱️ **예상 실행 시간**

- **개별 알고리즘**: 5-10분
- **통합 비교 실험**: 30-40분
- **서버 부하**: 최대 초당 1,000개 요청

## 🏆 **성공 기준**

- ✅ **95% 이상 검증률**: 모든 알고리즘
- ✅ **성능 비교 완료**: 4개 알고리즘 상호 비교
- ✅ **실용성 검증**: IoT 환경 적합성 확인
- ✅ **보안성 분석**: 각 알고리즘 장단점 분석

---

**실험 준비 완료! 다음 명령어로 시작하세요:**

```bash
python multi_algorithm_experiment.py
```